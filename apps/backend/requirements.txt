fastapi==0.120.4
# fastapi 0.120+ pulls in annotated-doc (formerly typing-extensions only).
# Pinning explicitly so a future fastapi bump cannot silently drop it.
annotated-doc==0.0.4
uvicorn[standard]==0.32.1
# starlette 0.49.x bundles the CVE-2025-62727 Range-header DoS fix.
# fastapi 0.120.x is the first line that admits starlette<0.50, so the
# fastapi bump above is a precondition for this pin.
starlette==0.49.3
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
celery[redis]==5.4.0
redis==5.2.0
structlog==24.4.0
httpx==0.28.1
# python-multipart 0.0.27 fixes CVE-2026-42561 (DoS via boundary parsing in
# multipart/form-data). 0.0.22 still ships the regression. 0.0.20 was the
# original CVE-2026-24486 path-traversal fix line.
python-multipart==0.0.27
# Auth — Phase 1 PR #5.
# bcrypt is pinned to 4.0.1 because passlib 1.7.4 is incompatible with
# bcrypt >= 4.1 (passlib reads bcrypt.__about__.__version__, which 4.1+ removed).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# python-jose 3.5.0 widened pyasn1 to >=0.5.0 so we can take the pyasn1 0.6.3
# fix (CVE-2026-30922, ASN.1 unbounded-recursion DoS). 3.4.0 still pinned
# pyasn1<0.5.0, which blocked the bump.
python-jose[cryptography]==3.5.0  # H-1 CVE fix (algorithm confusion / DoS) + pyasn1 0.6.x compat
# Transitive of python-jose / rsa. Pinned explicitly so Trivy / pip-audit
# can verify the CVE-2026-30922 fix on a clean image rebuild.
pyasn1==0.6.3
slowapi==0.1.9
email-validator==2.2.0
